{
  "name": "quiz-project-nt",
  "version": "1.0.0",
  "private": true,
  "description": "SaaS exam platform — NestJS backend + Next.js frontend monorepo",
  "scripts": {
    "install:all": "npm --prefix apps/backend ci && npm --prefix apps/frontend ci",
    "dev:backend": "npm --prefix apps/backend run start:dev",
    "dev:frontend": "npm --prefix apps/frontend run dev",
    "build": "npm --prefix apps/backend run build && npm --prefix apps/frontend run build",
    "build:backend": "npm --prefix apps/backend run build",
    "build:frontend": "npm --prefix apps/frontend run build",
    "test": "npm --prefix apps/backend test",
    "lint": "npm --prefix apps/backend run lint",
    "migration:run": "npm --prefix apps/backend run migration:run",
    "seed": "npm --prefix apps/backend run seed"
  }
}
